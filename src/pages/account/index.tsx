import React, {FC} from 'react';
import AccountLayout from "../../layouts/accountLayout";

const Account : FC = () => {
    return (
        <AccountLayout>

        </AccountLayout>
    );
};

export default Account;